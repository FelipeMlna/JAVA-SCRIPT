alert( 2 > 1 );  // true (correct)
alert( 2 == 1 ); // false (wrong)
alert( 2 != 1 ); // true (correct)

// TAREA
5 > 4
"apple" > "pineapple"
"2" > "12"
undefined == null
undefined === null
null == "\n0\n"
null === +"\n0\n"

//SOLUCION
5 > 4 → true // CIERTAMENTE 5 ES MAYOR QUE 4.

"apple" > "pineapple" → false //"A" ES MENOR QUE "P".

"2" > "12" → true //"2" QUE ES EL PRIMER NUMERO ES MAYOR QUE "1" QUE ES EL PRIMER NUMERO DE LAS SEGUNDAS COMILLAS.

undefined == null → true //Los valores nullson undefinediguales entre sí únicamente.

undefined === null → false //La igualdad estricta es estricta. Diferentes tipos de ambos lados conducen a falso.

null == "\n0\n" → false //Similar a (4), null sólo que es igual a undefined.

null === +"\n0\n" → false //Igualdad estricta de diferentes tipos.